import LoginCard from "@/features/auth/components/LoginCard.tsx";

function LoginPage() {
    return (
        <section className="container flex h-full w-full flex-col items-center justify-center px-4 text-center">
            <LoginCard />
        </section>
    );
}

export default LoginPage;